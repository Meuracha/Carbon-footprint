FROM apache/airflow:2.8.1

# apache/airflow:2.8.1 ใช้ Python 3.8
# ต้องใช้ package versions ที่ compatible กับ Python 3.8

RUN pip install --no-cache-dir \
    dbt-postgres==1.7.4 \
    requests==2.31.0 \
    pandas==2.0.3 \
    pyarrow==12.0.1